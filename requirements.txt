# Requirements for CQC-Accelerator project

# Note: Ensure Python version 3.8 or higher is installed on your system before proceeding.
# You can check your Python version with 'python --version' or 'python3 --version'.

# Core dependencies
pandas>=1.3.0
numpy>=1.21.0
matplotlib>=3.4.0
seaborn

# GUI framework
streamlit>=1.0.0

# Database backend
# Note: SQLite is included in Python's standard library. Ensure your Python installation includes it.
# No separate installation is required for SQLite.

# Machine Learning
scikit-learn>=1.0.0

# Testing and development tools
pytest>=6.2.0
black>=21.7b0

# Optional for scaling (uncomment if using Spark)
# pyspark>=3.2.0

# Note: Additional dependencies may be added as the project evolves.
